// import { AppInitializer } from '../../src/index';
import { defMiddlewares, defGuards } from '../middlewares/index';

// const appInitializer: AppInitializer = (middlewares: {
//   express: typeof defMiddlewares;
//   guards: typeof defGuards;
// }) => {
//   return [{ guard: '' }];
// };

// const getAppConfig: AppConfig[] = () => [
//   {
//     middleware:
//   }
// ];

// export default appConfig: AppConfig[] = getAppConfig();
